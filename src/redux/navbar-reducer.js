let initialState = {
    bestFriends: [
        { id: 1, name: "Alex" },
        { id: 2, name: "Peter" },
        { id: 3, name: "Andrew" }
    ]
};

let navbarReducer = (state = initialState, action) => {

    return state;
}

export default navbarReducer;